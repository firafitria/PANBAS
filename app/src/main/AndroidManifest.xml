<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.panbas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PANBAS">
        <activity android:name="com.dicoding.panbas.ui.detail_report.DetailReportActivity"></activity>
        <activity android:name="com.dicoding.panbas.ui.detail.DetailActivity" />
        <activity android:name="com.dicoding.panbas.ui.form.FormActivity" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.dicoding.panbas.ui.main.MainActivity" />
    </application>

</manifest>